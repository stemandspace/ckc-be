'use strict';

/**
 * discovery-jar-answer service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discovery-jar-answer.discovery-jar-answer');
