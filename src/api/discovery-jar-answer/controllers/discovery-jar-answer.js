'use strict';

/**
 * discovery-jar-answer controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::discovery-jar-answer.discovery-jar-answer');
