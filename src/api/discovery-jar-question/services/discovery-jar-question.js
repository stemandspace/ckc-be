'use strict';

/**
 * discovery-jar-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::discovery-jar-question.discovery-jar-question');
