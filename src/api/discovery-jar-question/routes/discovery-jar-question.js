'use strict';

/**
 * discovery-jar-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::discovery-jar-question.discovery-jar-question');
