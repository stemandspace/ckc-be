'use strict';

/**
 * just-launched-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::just-launched-config.just-launched-config');
