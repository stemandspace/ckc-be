'use strict';

/**
 * time-tracking-reward router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::time-tracking-reward.time-tracking-reward');
