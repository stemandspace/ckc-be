'use strict';

/**
 * time-tracking-reward controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::time-tracking-reward.time-tracking-reward');
