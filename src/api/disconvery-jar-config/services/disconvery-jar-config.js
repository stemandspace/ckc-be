'use strict';

/**
 * disconvery-jar-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::disconvery-jar-config.disconvery-jar-config');
