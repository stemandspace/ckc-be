'use strict';

/**
 * disconvery-jar-config controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::disconvery-jar-config.disconvery-jar-config');
