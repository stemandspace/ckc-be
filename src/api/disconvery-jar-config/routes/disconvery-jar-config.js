'use strict';

/**
 * disconvery-jar-config router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::disconvery-jar-config.disconvery-jar-config');
